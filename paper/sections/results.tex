% results.tex
%
% Subsections:
%   6.1  Convergence Curves
%   6.2  Cost Efficiency
%   6.3  Statistical Significance
%   6.4  Enhancement Attribution

\section{Results}
\label{sec:results}

% NOTE: All \NUM{} placeholders below require benchmark data from Task 5.3/5.4.
\TODO{This entire section requires benchmark data. Run Task 5.3 (benchmark suite)
and Task 5.4 (statistical analysis) before writing narrative prose.
The structure and figure/table placeholders below are correct --- fill in numbers.}

% --- 6.1 Convergence Curves ---
\subsection{Convergence Curves}
\label{sec:results-convergence}

\TODO{Write narrative for convergence curves. Key questions to answer:
  (1) Does Treatment converge faster than Control?
  (2) Is the gap consistent across domains?
  (3) Do any configurations plateau early?}

\begin{figure}[h]
  \centering
  % \includegraphics[width=\linewidth]{figures/convergence_curves.pdf}
  \TODO{Insert convergence curves figure (generated by analysis script).
    X-axis: experiment number (0-50).
    Y-axis: normalized score (0-1).
    Lines: one per configuration (Raw, Greedy, Control, Treatment).
    Bands: 95\% bootstrap CI across 10 runs.
    Facets: one per benchmark target (B1-B5).}
  \caption{Convergence curves for all configurations across five benchmark
    targets. Bands indicate 95\% bootstrap confidence intervals over 10 runs.
    \NUM{Treatment converges to higher scores in X/5 targets.}}
  \label{fig:convergence}
\end{figure}

% --- 6.2 Cost Efficiency ---
\subsection{Cost Efficiency}
\label{sec:results-cost}

\TODO{Write narrative for cost efficiency. Key questions:
  (1) At equal USD budget, which configuration performs best?
  (2) Does the dual-agent cost overhead pay off?
  (3) What is the crossover point where Treatment becomes cost-efficient?}

\begin{figure}[h]
  \centering
  % \includegraphics[width=\linewidth]{figures/cost_efficiency.pdf}
  \TODO{Insert cost efficiency figure.
    X-axis: cumulative cost in USD.
    Y-axis: normalized score.
    Lines: one per configuration.
    Use log scale on X-axis.}
  \caption{Cost efficiency comparison: score as a function of cumulative
    API spend. Treatment reaches comparable score at \NUM{X}\% of the cost
    required by the greedy baseline on \NUM{X}/5 targets.}
  \label{fig:cost-efficiency}
\end{figure}

% --- 6.3 Statistical Significance ---
\subsection{Statistical Significance}
\label{sec:results-stats}

\Cref{tab:significance} reports the results of Wilcoxon signed-rank tests
comparing Control vs. Treatment on each benchmark target, with Holm-Bonferroni
correction applied across the five comparisons.

\begin{table}[h]
  \centering
  \caption{Statistical comparison: Control vs. Treatment.
    $p$-values are Holm-Bonferroni adjusted.
    Effect sizes are Cohen's $d$ with 95\% bootstrap CI.
    $\dagger$ = significant at $\alpha = 0.05$ after correction.}
  \label{tab:significance}
  \begin{tabular}{lcccccc}
    \toprule
    & \multicolumn{2}{c}{\textbf{Score (median)}} & & & & \\
    \cmidrule(lr){2-3}
    \textbf{Target} & \textbf{Control} & \textbf{Treatment}
      & \textbf{$W$} & \textbf{$p_{\text{adj}}$} & \textbf{Cohen's $d$} & \textbf{Sig.} \\
    \midrule
    B1 & \NUM{0.XX} & \NUM{0.XX} & \NUM{XX} & \NUM{0.XXX} & \NUM{X.X} & \NUM{$\dagger$} \\
    B2 & \NUM{0.XX} & \NUM{0.XX} & \NUM{XX} & \NUM{0.XXX} & \NUM{X.X} & \NUM{$\dagger$} \\
    B3 & \NUM{0.XX} & \NUM{0.XX} & \NUM{XX} & \NUM{0.XXX} & \NUM{X.X} & \NUM{---} \\
    B4 & \NUM{0.XX} & \NUM{0.XX} & \NUM{XX} & \NUM{0.XXX} & \NUM{X.X} & \NUM{$\dagger$} \\
    B5 & \NUM{0.XX} & \NUM{0.XX} & \NUM{XX} & \NUM{0.XXX} & \NUM{X.X} & \NUM{$\dagger$} \\
    \bottomrule
  \end{tabular}
\end{table}

\TODO{Write 2-3 paragraphs interpreting the significance table.
  Include: which targets show largest effect size, any surprising non-results,
  interpretation of Holm-Bonferroni adjusted $p$-values.}

% --- 6.4 Enhancement Attribution ---
\subsection{Enhancement Attribution}
\label{sec:results-attribution}

\TODO{Write enhancement attribution analysis. For each major enhancement,
report the delta in a key metric when that enhancement is ablated from Treatment.
Format as a table or bar chart.}

\begin{table}[h]
  \centering
  \caption{Enhancement attribution: delta in final score ($\Delta\text{score}_{50}$)
    when each enhancement track is ablated from Treatment.
    Negative values indicate the enhancement contributes positively.}
  \label{tab:attribution}
  \begin{tabular}{lcc}
    \toprule
    \textbf{Ablation} & \textbf{$\Delta\text{score}_{50}$} & \textbf{$\Delta\text{cost}$} \\
    \midrule
    Treatment $-$ Dual-Agent     & \NUM{$-$0.0X} & \NUM{$-$XX\%} \\
    Treatment $-$ Two-Phase      & \NUM{$-$0.0X} & \NUM{---} \\
    Treatment $-$ Lineage        & \NUM{$-$0.0X} & \NUM{---} \\
    Treatment $-$ Episodic Mem.  & \NUM{$-$0.0X} & \NUM{---} \\
    Treatment $-$ Adaptive Samp. & \NUM{---}     & \NUM{$+$XX\%} \\
    \bottomrule
  \end{tabular}
\end{table}

\TODO{Write 2-3 paragraphs discussing attribution results. Which enhancements
contribute most to score improvement? Which to cost reduction? Any unexpected
interactions between enhancements?}
